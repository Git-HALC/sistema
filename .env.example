# ============================================================================
# Sistema DM - variaveis de ambiente
# Copie para .env e preencha. Nao versionar .env!
# ============================================================================

# Chave AES-256 para cifrar credenciais fiscais (senha prefeitura, certificado).
# Gerar com: php -r "echo bin2hex(random_bytes(32));"
FISCAL_ENCRYPTION_KEY=
